1、select() 
2、selectAll()
3、text()
4、append()
5、insert() 会在所有集合里插入，如果没有第二个css参数，默认在最后插入（等同于append），否则在第二个css选择器前面插入
6、remove()
7、domain()
8、range()
9、min()
10、max()
11、scaleLinear()
12、scaleBand()
13、update、enter、exit

关于扇形图的绘制：
1、原始数组数据 通过 布局 转换为 相关数学数据
2、使用生成器，进行相应配置
3、append g元素，然后在g元素中分别添加 path 添加的 path d 属性的值 由 生成器传入绑定的 d 数据 值进行构造+return


